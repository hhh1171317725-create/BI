# Bid Monitor

Entry: `/tools` -> 出价监测 (`/bid-monitor.html`). No extra Nginx route is required. The existing static file handler serves the page; Spring handles `/api/bid-monitor/**`. The snapshot endpoint automatically creates `bid_monitor_snapshots` on first use; the database user needs CREATE TABLE permission.

## Deploy

Run from a server terminal independent of the website:

```bash
cd /www/wwwroot/BI
git pull --ff-only origin main
export JAVA_HOME=/www/server/java/jdk-21.0.2
export PATH="$JAVA_HOME/bin:$PATH"
./mvnw package && systemctl restart dahanghai-analysis
```

Only restart after a successful build. Administrators can enable `bidMonitor` for operators in the existing tool permission dialog. All endpoints enforce login and tool permission. The query-and-enable action saves encrypted credentials for reuse and scheduling; snapshots contain only allowlisted plan data. Snapshot reads always use the current website user; configuration changes also verify the website user bound when the page was loaded.

## DingTalk Task TOP5 (2.2.0)

- Open the 钉钉任务 TOP5 section below saved task prices. Select 1-10 saved tasks and enter the destination robot Webhook, optional signing secret, and matching security keyword. Default daily time is 18:00 in Asia/Shanghai. Enable 定时推送 and save. Different website users have independent configurations; this feature does not use the shared email-forwarding robot.
- Robot setup references: [official custom robot access](https://open.dingtalk.com/document/robots/custom-robot-access) and [security settings](https://open.dingtalk.com/document/robots/customize-robot-security-settings). Webhook and signing secrets are stored as an owner-bound encrypted bundle using the existing bid-monitor key. They are never returned to the browser. Blank credential fields retain saved values; changing a robot requires a new signing secret or explicitly selecting 不使用加签. Clearing the robot removes its credentials and disables notification scheduling, without deleting data sync credentials or task prices.
- 预览 TOP5 shows the exact per-task message without posting it. 立即推送 requires confirmation and uses the same saved configuration and snapshot as scheduled sending. It is a real send, not a sandbox, and can duplicate the daily message if used intentionally. Manual sends are limited to one attempt per five minutes per website user.
- Each selected task gets one text message containing up to five plans, sorted by total spend descending. Matching uses saved account-name keywords and excludes ambiguous matches. Scope is the current user's collected global top-400 snapshot, not all upstream plans. A task with fewer than five matches sends its actual count; no matches are explicitly labeled as absent from the collected scope. Only same-day, top-400 snapshots no older than 20 minutes are accepted. Keep the existing ten-minute server sync enabled; historical queries and Excel imports do not become scheduled notification data.
- Messages include task, snapshot/statistical time, plan/account names and IDs, spend, cash spend, registrations, conversions, commission, estimated grant, current bid, break-even bid, three-decimal cash ROI and bid profit rate. Calculations follow the existing page rules using decimal arithmetic in Java.
- The server checks due notifications every 15 seconds using a bounded two-worker pool. Schedule and delivery attempt state are persisted separately from data-sync scheduling in the same per-user JSON record. The database must support JSON_EXTRACT. No new tables or runtime keys are required. The process can recover a missed slot only within 30 minutes of that day's configured time; older missed slots are skipped rather than sending unexpected catch-up messages.
- A durable daily claim is recorded before external delivery. Restart, concurrent schedulers and uncertain transport outcomes cannot automatically replay that daily batch. Partial failures keep the acknowledged message count; remaining tasks are not automatically retried that day. A crash after claiming may leave an uncertain result that must be checked in the group. Exactly-once delivery cannot be guaranteed by a webhook. Config/price/permission changes are checked before each message; messages already in flight cannot be recalled. Snapshot freshness, revoked access or invalid credentials result in a visible failure without sending stale data.
- Deployment requires `./mvnw package` and a service restart because these endpoints and the daily scheduler are new. Merely pulling the frontend files is not sufficient.

## Server Scheduled Updates (2.1.3)

1. No extension is required. Stop the old extension's bid synchronization, or disable that extension if it is no longer needed. The new page talks only to the BI backend.
2. Enter Cookie, client-user and main-user-id from the same authorized Chuangliang session. Each operator uses their own website account and their own upstream credentials. No sample or historical credentials are automatically saved.
3. Updates run every 10 minutes. Creation scope is Beijing today minus three days through today, inclusive (four calendar dates): September 4 reads September 1-4. Existing schedules adopt the interval on the next save or successful run. Click 查询并启用定时更新 to save credentials, query immediately and schedule the next server run in 10 minutes. Blank Cookie/client-user/main-user-id values reuse this website user's saved values; changing the upstream identity requires a fresh Cookie. Plaintext is cleared after saving and is never stored in browser storage. Refreshing the page restores saved identity fields, never the Cookie.
4. 保存并启用 remains available to queue an immediate server run (approximately 5 seconds), while 查询并启用定时更新 performs its first query directly through the owned saved-credential endpoint. Subsequent manual queries with unchanged saved credentials do not postpone or invalidate an enabled schedule. Schedules persist in MySQL and continue with Chrome closed. 立即同步 queues another run; 停止同步 cancels future scheduling and invalidates in-flight commits. 清除保存凭据 removes the encrypted Cookie and query revision while preserving task prices and the last snapshot.
5. Each run reads Beijing's current-day statistics, ordered by stat_cost descending, selecting min(400, upstreamTotal) plans in at most four 100-row pages. The previous full-collection behavior is replaced. The creation window remains today minus three days through today, inclusive. The snapshot records spend_desc_top_400 and the full upstream match count; progress targets the selected count, not the upstream total. Old full snapshots remain explicitly labeled as historical until a successful new sync replaces them. No fifth page is requested to fill duplicates or missing data.
6. Failed authentication, upstream rejection/verification, undecryptable credentials or revoked website tool access pause server synchronization without replacing the last successful snapshot. Network failures and incomplete/duplicate/empty data preserve old data and retry in 10 minutes without requiring credential re-entry. Correct expired credentials and enable again; login, verification and upstream permissions are not bypassed.
7. A bounded two-worker pool performs collection outside the Spring scheduler thread. A database row lock and revision token guard each claim/commit. Interrupted jobs have a three-minute lease before recovery. Successful page progress renews the lease, checks ownership/permission and exposes the collected/total count; stopping prevents further pages and commits. Permission and Beijing date are checked again before saving. Enabled server jobs reject old extension snapshot uploads to avoid overwrites.
8. Visible pages poll status every 5 seconds, independently of server scheduling. Manual queries matching today's default statistics/creation window follow subsequent successful server snapshots automatically. Historical queries and Excel imports are not replaced automatically. 读取最新快照 explicitly loads the latest owned snapshot; its timestamp and statistical date remain visible. Every saved-credential page request verifies the website user and credential revision before and after fetching, rejecting account switches or cleared/replaced credentials mid-query.

### Credential Storage And Deployment

- The database user needs CREATE TABLE permission for bid_monitor_server_sync and bid_monitor_snapshots. Settings, due time and status are persisted per website user. API responses never include encrypted or plaintext Cookie values.
- AES-256-GCM uses a random nonce per save and binds ciphertext to the website user ID. The 32-byte key is created in DHH_RUNTIME_DIR/bid-monitor.key (default .runtime/bid-monitor.key). On POSIX it is created owner-only. The service user must be able to read/write this directory. Keep it outside the Nginx static root.
- Back up the key securely alongside database backups. Do not commit it or delete the runtime directory during deployment. If the key is lost, restore it or re-enter credentials for affected users. Multiple application instances must share the same key and MySQL database.
- Use HTTPS for the website and keep its normal authentication enabled. Encryption at rest is not protection against a compromised application server.
- Credentials remain subject to expiry and upstream access policy. Local authorized HTTP success does not guarantee that the deployment server's IP will be accepted. A server-side refusal requires checking the provider's permissions or supported integration, not fingerprint evasion.

## Data And Interpretation

- In 任务结算价格, add a task name, a substring to match in the upstream account name, and its customer settlement price per registration. Save task prices. Rules are stored per website user with optimistic conflict detection and never change another user's rules. Up to 50 tasks are supported. Matching is case-insensitive; zero matches, multiple matches or invalid prices suppress ROI for those plans rather than silently choosing a price. There is no global fallback price. The task filter, search, table and CSV use the same resolved prices.
- `convert_cnt / active_register` is the observed return ratio; it is not necessarily the configured return rate, especially during delayed attribution.
- This assumes `cpa_bid` targets exactly the conversion counted in `convert_cnt`, and all registrations are billable at the entered price. Deep-conversion and dual-bid campaigns need a different analysis.
- ROI now uses commission / cash spend, rounded to three decimal places. Commission = registrations * task settlement price. Conversion cost = total spend / conversions. A plan receives a calculated grant only when conversion cost > 1.2 * bid AND conversions > 6; then grant = total spend - bid * conversions, otherwise grant = 0. Cash spend = total spend - calculated grant. Equality at either threshold does not qualify. Binary-rounding noise at the 1.2 boundary is ignored. Grants are estimates from this rule, not confirmation that the platform has credited them. Unknown required inputs or zero cash spend give -- rather than Infinity. Zero conversions imply no grant, but ROI can still be calculated when commission and positive cash spend are known.
- 出价利润率 compares the bid to its break-even line: break-even bid = task settlement price / (conversions / registrations); bid profit rate = (break-even bid - current bid) / break-even bid. For a break-even bid of 100, bids of 80 / 100 / 120 imply 20% / 0% / -20%. This is a bid-based margin, not cash ROI minus one. The display uses two percentage decimals; an undefined break-even line gives --. Hovering shows the break-even bid; ROI hover shows commission, calculated grant and cash spend. CSV includes these calculation inputs too.
- Date ranges including today are marked provisional. Yesterday can still have delayed attribution; review after the project's attribution window closes.
- Results describe the dates captured when data was fetched/imported, not subsequently edited date controls.
- Both manual queries and scheduled sync request at most four pages, by stat_cost descending, and validate exactly min(400, upstreamTotal) rows. Missing pages, duplicate plan IDs and changing totals cause errors without replacing previous results. Excel import is unchanged. `getSum` is not mixed with list data; summaries are calculated from the currently filtered rows. Grant eligibility is evaluated for each plan before aggregation. Aggregate ROI is total commission divided by total cash spend, not a ratio computed by applying the grant rule to totals, and not an average of plan ROIs. Aggregate bid profit rate is commission-weighted, equivalent to (total commission - total bid * conversions) / total commission for rows with valid break-even lines. If any selected row lacks a required value, that aggregate is unavailable instead of treating it as zero.
- Excel import reads the first sheet with header row first. Required columns: `stat_cost`, `convert_cnt`, `active_register`, `cpa_bid`, or 消耗、转化数、注册数、出价. Use a plan-level period aggregate without totals or duplicate rows, and select its actual period before importing. Keep account/plan IDs formatted as text.

## Verification

Version 2.2.0: the package build passed 160 Java tests; 36 JavaScript tests and the isolated desktop/mobile UI test passed. New coverage includes daily 18:00 Beijing scheduling, durable claims across restart, per-user encrypted configuration, permission checks, stale snapshot rejection, per-task descending TOP5 selection, cash ROI and break-even calculations, partial failure preservation, stop/price-change interruption, signing destination restrictions, nonzero/missing response codes, Spring construction, and the separate database due query. UI tests mock the robot endpoints and verify save, task selection, credential clearing, preview without delivery and explicitly confirmed sending. No real group delivery or production configuration was performed.

Version 2.1.3: 36 JavaScript tests and the isolated desktop/mobile UI test passed for the new cash ROI and bid-profit-rate calculations. Tests cover both strict grant thresholds, floating-point equality, zero conversions, zero cash spend, missing inputs/prices, per-plan grant aggregation, ROI sorting, CSV outputs, and bids below/equal/above the break-even line. This is a frontend calculation change; saved snapshots, server query scope and the ten-minute schedule are unchanged. No live provider credentials were used for this verification.

Version 2.1.2: 145 Java tests, 32 JavaScript tests and the isolated headless Chrome UI test passed. Coverage includes saved credential reuse without a Cookie in paginated browser requests, website-owner and credential revision guards, unchanged manual queries preserving the scheduled deadline, transient failure retry, three-decimal ROI in rows/summary/CSV, removal of other computed outputs, default daily snapshot following, historical data preservation and desktop/mobile layout. These are local automated checks; the production deployment and its upstream authorization still need verification.

Version 2.1.1 changes selection to spend-descending top 400 while retaining the four-date creation scope and task pricing/profit/ROI features. Regression tests cover a 335,367-row upstream total with only four requests and 400 saved rows, a smaller 350-row result, progress capped at 400, fifth-page rejection, and incomplete/duplicate-page preservation.

Local authorized Java HTTP verification on 2026-09-03 returned exactly 400 rows in four pages from 3,338 matches in approximately 9 seconds, with selection spend_desc_top_400. The probe did not persist credentials or update a production snapshot. The package build passed 141 Java tests; 32 JavaScript tests and the no-extension desktop/mobile UI test passed.

Version 2.1.0: the actual Java collection method was tested locally with authorized credentials held only in memory. It fetched 3,335 of 3,335 matching plans over 34 pages in approximately 79 seconds, with creation dates 2026-08-31 through 2026-09-03 and statistics on 2026-09-03. A prior attempt failed before producing a complete snapshot; its exact cause was not captured. No production credentials, schedules or snapshots were saved by these probes. Deployment-server acceptance still needs verification. Automated tests cover September 4 -> September 1-4, month/year boundaries, full pagination, lease renewal/cancellation, owner-scoped task prices, ambiguous matches, and the projected profit/ROI formulas.

Version 2.0.0 verification on 2026-09-03: the new Java server collection method itself was invoked locally with the authorized sample credentials held only in memory. It fetched the top 200 from 4,669 matching plans in approximately 4 seconds through ordinary Java HTTP, without a browser or extension. This did not save credentials, enable a schedule or write a production snapshot. It does not establish acceptance of the deployment server's IP. The package build passed 135 Java tests; 29 JavaScript tests and the no-extension desktop/mobile UI test also passed.

Run `./mvnw package` for Java tests, including encrypted storage, owner binding, server task collection, stop/reconfigure guards, permission revocation and failure preservation. `node --test scripts/bid-monitor.test.cjs scripts/bid-sync.test.cjs scripts/bid-sync-worker.test.cjs` verifies formulas and legacy extension behavior. `node scripts/bid-monitor-ui-test.cjs` uses isolated headless Chrome with mocked APIs and no extension, verifying desktop/mobile layout, import, search, pagination, export and server synchronization controls. Production server access still requires deployment and a successful run using current authorized credentials.

Live comparison on 2026-09-03 using a user-supplied authorized request reproduced code -1 with a 46-character ff-request-id and code 0 after using the original 48-character format (Beijing timestamp + UUID hex + ff). Both Java and extension clients now generate this format. The successful response has data.list and data.page_info.total_count; deep_bid_type is an array. The list already includes stat_cost, convert_cnt, active_register and cpa_bid, so the separate getRealPromotionData request is not needed for these observed fields.

Historical 1.9.3 verification: creation-time sorting produced cross-page duplicates, so promotion_id descending was used for full collection. After fixing these differences and carrying total_count/total_page into subsequent extension requests, the actual extension parsing/validation core read all 4,656 rows over 47 pages in 54 seconds, without duplicates, for plans created 2026-08-28 through 2026-09-03 and statistics on 2026-09-03. This was a read-only local HTTP test, not a live end-to-end Chrome-to-BI upload. Browser orchestration and persistence are covered by isolated/mocked tests; the deployed user session still needs a successful browser sync. No supplied cookies are committed or persisted by the application.

Version 1.9.4 was verified with the live read-only API: 4,656 matching plans, two requests, 200 rows, approximately 3 seconds, using stat_cost descending. This is not a live Chrome-to-BI upload test. Ranking changes can still cause duplicates between pages; the original failure protections remain rather than requesting a third page.
